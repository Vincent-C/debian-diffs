Tarball: orig tarball is actually empty; src:meta-gnome3 just provides a collection of meta-packages.

Changes from upstream Debian meta-packages:
  * Remove dependency on:
    - evolution*
    - tomboy
    - iceweasel, xul-ext*
    - gnash
    - libreoffice*
    - tracker*, and dependencies:
      - gnome-boxes, gnome-documents, gnome-music, gnome-photos
